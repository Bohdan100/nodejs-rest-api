const isValidId = require("./isValid");
const isAuth = require("./isAuth");
const validateBody = require("./validateBody");
const { isUpload } = require("./isUpload");

module.exports = {
  isValidId,
  isAuth,
  validateBody,
  isUpload,
};
